[phases.setup]
nixPkgs = ["php", "composer"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.build]
cmds = ["echo 'Build completed - assets already compiled'"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
